import Section from "@/components/Section";

const AboutSection = () => (
  <Section title="About" className="pb-0" center maxWidthClass="max-w-3xl">
    <p className="max-w-3xl">
      This playbook was built with service advisors, controllers, and shop
      foremen who wanted lien protection without slowing the lane. Everything is
      designed to run on tablets, print stations, or SMS/email so every team
      member can follow the same release rules.
    </p>

    <p className="max-w-3xl mt-4">
      We iterate with dealership groups, independent shops, and warranty
      administrators to keep the workflows compliant and practical. If you need
      a tweak for your state or OEM requirements, we can codify it quickly.
    </p>
  </Section>
);

export default AboutSection;
